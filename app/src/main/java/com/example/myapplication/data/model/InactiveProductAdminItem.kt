package com.example.myapplication.data.model

/** Admin: inactive product (`isActive == false`) list row. */
data class InactiveProductAdminItem(
    val productId: String,
    val name: String,
    val storeId: String,
)
